# Design QA — V4.1 implementation

- 実施日: 2026-09-12
- 対象: トップ、サービス一覧、Googleマップ、LP、業務アプリ、追客、お問い合わせ、プライバシー、特商法、404、旧LP URL（全11ルート）
- 基準コミット: f469074a75b5ced83196b0d4608acc53dc9a3051
- 状態: ローカル実装・検査済み。以下は公開前検証の記録
- 参照: 承認済みV4.1カンプ。生成画像を個別素材化し、文字・リンク・入力欄はHTMLで実装

## 確認したこと

- 静的検査: 11ルート、見出し118件、src/href参照587件。見出しの句点・ID重複・内部リンク/画像参照の欠落なし
- `node --check assets/redesign/site.js` と `git diff --check` を実施
- プライバシー本文と特商法の取引条件は基準コミットと同一
- 320px、768pxで表示ルート10件を巡回し、横はみ出しなし。旧URLはLPへ即時転送
- 390pxでトップ・運営者・追客・フォームを目視。PCではトップ・サービス詳細・お問い合わせ・法務・404を目視
- メニューの開閉、Escape、フォーカス復帰を確認
- メールフォームのサービス選択引継ぎ、未入力エラー、エラー箇所へのフォーカス、修正後の相談内容コピー、入力保持を確認
- 実際のメール送信は未実施。メールアプリ経由での送信であり、Web上では送信成功を表示しない
- 新しいJavaScriptに外部送信・ブラウザ永続保存処理なし。自社配信のCSS・JS・フォント・画像のみを使用
- 動きはヒーロー写真の4秒・1回とフローの1回の強調。prefers-reduced-motionで停止するCSSを確認

## 視覚比較と修正

- トップと追客のカンプ・実装を同じ表示幅に正規化し、同一比較画面で確認
- 温かい店舗写真、クリーム地、深緑、明朝見出し、サービス別の画像、内容を追いやすい縦の順序を引継ぎ
- 誤解を避けるため、生成写真・架空画面にはイメージ表記。実績3件と運営者写真は原本を使用
- スマホでの見出し末尾1文字の折返し、料金の数値と税込表記、困りごとカード内の改行を修正
- カンプ画像の小さな文字をそのまま拡大せず、実ブラウザで本文16px、入力欄16px、主要ボタン44px以上を基準に調整
- 長いフルページ画像の取得ではブラウザの合成が乱れたため、それは合否判定に使用せず、画面内スクリーンショットとDOM実測で確認
- 画像取得が止まったプレビュータブは開き直し、対象ページの表示を確認

## 公開ゲート

1. LPの修正回数は2026-09-12に運営者承認済み。「公開前2回まで無料、3回目以降は別途見積もり」に統一
2. GitHub CodeQLの実行・成功確認を公開条件とする。実行結果は対象プルリクエストのチェックで確認する
3. mainの最新状態を再確認してから非強制・履歴を残す公開を行う
4. GitHub Pagesの成功、本番URL・各ルート・フォーム導線を公開後に再確認する

## 復元・引継ぎ

- 元のHTML、CSS、JavaScript、画像と履歴は基準コミットで復元可能
- 既存のドメイン、公開ワークフロー、認証、解析サービスは変更していない
- 新規共通ファイル: assets/redesign/site.css、assets/redesign/site.js
- 新規写真8点はWebP化。アイコンはPhosphor Icons、見出しはNoto Serif JP。各ライセンスを同梱
- フォントは現在の公開文面の文字に絞ったサブセット。新しい見出し追加時はフォントサブセットを更新するかフォールバック表示を確認する
- 以前の2026-08-22 QAは履歴に残り、このレポートの対象とは異なる

local validation result: pass

この記録は公開前のスナップショット。CodeQL・GitHub Pages・公開後検証の結果は公開作業時に別途記録し、ローカル検査の成功を公開成功として扱わない。
